/**
 * # Application configuration
 *
 * This is the main application configuration module. It is a normal module
 * using AMD syntax.
 */

define(function(require) {
  return {
    name: 'Ribcage app'
  };
});
